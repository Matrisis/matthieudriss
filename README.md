# Audit Claude Code & Agentic Dev

Landing page — French B2B lead generation. Single-page static site.

## Run locally

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`.

No build step, no dependencies.
